import Header from "../components/header"

const AboutPage = () => {
    return `
        ${Header()}
        <main>
            <h1>About Page</h1>
        </main>
        <footer></footer>
    `
}

export default AboutPage